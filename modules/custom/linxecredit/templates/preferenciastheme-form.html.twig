                                    {% if form['#myvars'].showPreferenciaForm == false  %}
                                        {{drupal_entity('block_content', '23') }}
                                    {% else %}     
                                        <form class="formularioin" action="" name="set-preferencias" id="set-preferencias">
                                            
                                            <div class="form-row mt-5">
                                                    {{ form.categorias }}
                                            </div>
                                            <div class="form-row">
                                                <div class="col-12 px-3 pt-3">
                                                    <div class="form-group">
                                                        {{ form.mensaje }}
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="form-row d-flex justify-content-center">
                                                <div class="col-md-7 px-3 text-center">
                                                    {{ form.actions.submit }}
                                                    {{ form.form_build_id }}
                                                    {{ form.form_token }}
                                                    {{ form.form_id }}
                                                </div>
                                                <div id="errorMsj"></div>
                                            </div>
                                        </form>
                                    {% endif %}
                                    